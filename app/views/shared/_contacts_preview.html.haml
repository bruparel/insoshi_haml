- unless @some_contacts.empty?
  %h2
    Contacts
  %ul.grid.contacts.small
    - @some_contacts.each do |contact|
      = render :partial => 'shared/contact', :locals => {:contact => contact}
    %li.more
      = link_to "All contacts &raquo;", person_connections_path(person)
